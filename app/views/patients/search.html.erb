 <ul>
    <% if @patients.empty?  %>
      No patients found with that first or last name!
    <% else  %>
Your search returned <font color='red'><%= @patients.count  %></font> patients
    <% @patients.each do |patient| %>
      <li><%= link_to patient.patient_full_name,
      :action => 'show', :id => patient.id  %></li>
  <% end %>
  <% end %>
 </ul>
